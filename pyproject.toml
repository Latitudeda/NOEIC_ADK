[build-system]
requires = []
build-backend = "flit_core.buildapi"

[project]
name = "Pinghu ADK documentation"
authors = [{ name = "kevinchen", email = "kevin_chen@Latitudeda.com" }]
dynamic = ["version", "description"]
